# Advanced Usage

Read the canonical user documentation before answering:

- [Default keybindings](../../../../docs/keybindings.md)
- [Git mode](../../../../docs/git-mode.md)

Explain workflows as visible layers and transitions rather than reproducing the keybinding tables.
State whether an action is read-only or mutating and how `<C-q>` returns from it. For Git problems,
distinguish native Diffview rendering from BeckNvim's bounded loader and panel lifecycle. Never claim
that Git mode adds title highlights, automatic child selection, historical cursor restoration, or a
custom fold dispatcher.

For proxy-backed GitHub or translation failures, verify the active route without exposing tokens.
Git object inspection continues to use native Git and never fetches implicitly.
