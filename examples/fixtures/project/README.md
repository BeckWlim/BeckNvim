# Workspace notes

A documentation workspace with a small Lua cache example.

- Explore the distributed-cache diagram in `docs/architecture.md`.
- Resize the editor to see the layout adapt to the available width.
- Switch between rendered Markdown and source editing.
- Open recent files from the project homepage.
